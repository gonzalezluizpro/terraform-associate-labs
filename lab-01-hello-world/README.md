# Lab 01 — Hello World

## What this does
Uses the Terraform `local` provider to create a text file on disk.
Demonstrates the full `init → plan → apply → destroy` workflow.

## Commands
```bash
terraform init
terraform plan
terraform apply
terraform destroy
